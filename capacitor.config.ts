import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.hoppingfoxcheer',
  appName: 'hopping-fox-cheer',
  webDir: 'dist'
};

export default config;
